from molsysmt.tools.molsysmt_MolSys.is_molsysmt_MolSys import _checking_form

## From atom

def get_atom_index_from_atom(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_index_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_atom_id_from_atom(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_id_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_atom_name_from_atom(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_name_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_atom_type_from_atom(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_type_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_index_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_index_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_id_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_id_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_name_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_name_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_type_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_type_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_name_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_name_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_index_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_index_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_id_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_id_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_type_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_type_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_name_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_name_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_index_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_index_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_id_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_id_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_type_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_type_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_index_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_index_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_id_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_id_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_name_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_name_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_type_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_type_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_index_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_index_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_id_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsymst.tools.molsysmt_Topology import get_entity_id_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_name_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_name_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_type_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_type_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_atoms_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_atoms_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_groups_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_groups_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_components_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_components_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_molecules_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_molecules_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_chains_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_chains_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_entities_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_entities_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_bonded_atoms_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_bonded_atoms_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_bond_index_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_bond_index_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_bonds_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_bonds_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_inner_bond_index_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_inner_bond_index_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_inner_bonded_atoms_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_inner_bonded_atoms_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_inner_bonds_from_atom (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_inner_bonds_from_atom as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_coordinates_from_atom(item, indices='all', frame_indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Trajectory import get_coordinates_from_atom as _get
    return _get(item.trajectory, indices=indices, frame_indices=frame_indices, check_form=False)

def get_frame_from_atom(item, indices='all', frame_indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Trajectory import get_frame_from_atom as _get
    return _get(item.trajectory, indices=indices, frame_indices=frame_indices, check_form=False)

def get_n_frames_from_atom(item, frame_indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Trajectory import get_n_frames_from_atom as _get
    return _get(item.trajectory, frame_indices=frame_indices, check_form=False)

## group

def get_atom_index_from_group(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_index_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_atom_id_from_group(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_id_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_atom_name_from_group(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_name_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_atom_type_from_group(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_type_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_index_from_group(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_index_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_id_from_group(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_id_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_name_from_group(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_name_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_type_from_group(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_type_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_name_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_name_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_index_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_index_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_id_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_id_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_type_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_type_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_name_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_name_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_index_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_index_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_id_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_id_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_type_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_type_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_index_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_index_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_id_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_id_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_name_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_name_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_type_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_type_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_index_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_index_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_id_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_id_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_name_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_name_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_type_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_type_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_atoms_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_atoms_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_groups_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_groups_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_components_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_components_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_molecules_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_molecules_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_chains_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_chains_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_entities_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_entities_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_bonds_from_group (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_bonds_from_group as _get
    return _get(item.topology, indices=indices, check_form=False)

## component

def get_atom_index_from_component(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_index_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_atom_id_from_component(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_id_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_atom_name_from_component(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_name_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_atom_type_from_component(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_type_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_index_from_component(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_index_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_id_from_component(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_id_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_name_from_component(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_name_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_type_from_component(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_type_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_name_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_name_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_index_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_index_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_id_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_id_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_type_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_type_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_name_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_name_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_index_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_index_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_id_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_id_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_type_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_type_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_index_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_index_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_id_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_id_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_name_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_name_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_type_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_type_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_index_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_index_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_id_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_id_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_name_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_name_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_type_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_type_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_atoms_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_atoms_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_groups_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_groups_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_components_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_components_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_molecules_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_molecules_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_chains_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_chains_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_entities_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_entities_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_bonds_from_component (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_bonds_from_component as _get
    return _get(item.topology, indices=indices, check_form=False)


## molecule

def get_atom_index_from_molecule(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_index_from_molecule as _get
    return _get(item.topology, indices=indices, frame_indices=frame_indices, check_form=False)

def get_atom_id_from_molecule(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_id_from_molecule as _get
    return _get(item.topology, indices=indices, frame_indices=frame_indices, check_form=False)

def get_atom_name_from_molecule(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_name_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_atom_type_from_molecule(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_type_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_index_from_molecule(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_index_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_id_from_molecule(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_id_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_name_from_molecule(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_name_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_type_from_molecule(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_type_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_name_from_molecule (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_name_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_index_from_molecule (item, indices='all', frame_indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_index_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_id_from_molecule (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_id_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_type_from_molecule (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_type_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_name_from_molecule (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_name_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_index_from_molecule (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_index_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_id_from_molecule (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_id_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_type_from_molecule (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_type_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_index_from_molecule (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_index_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_id_from_molecule (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_id_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_name_from_molecule (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_name_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_type_from_molecule (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_type_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_index_from_molecule (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_index_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_id_from_molecule (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_id_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_name_from_molecule (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_name_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_type_from_molecule (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_type_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_atoms_from_molecule (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_atoms_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_groups_from_molecule (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_groups_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_components_from_molecule (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_components_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_molecules_from_molecule (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_molecules_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_chains_from_molecule (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_chains_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_entities_from_molecule (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_entities_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_bonds_from_molecule (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_bonds_from_molecule as _get
    return _get(item.topology, indices=indices, check_form=False)

## chain

def get_atom_index_from_chain(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_index_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_atom_id_from_chain(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_id_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_atom_name_from_chain(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_name_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_atom_type_from_chain(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_type_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_index_from_chain(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_index_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_id_from_chain(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_id_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_name_from_chain(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_name_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_type_from_chain(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_type_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_name_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_name_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_index_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_index_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_id_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_id_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_type_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_type_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_name_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_name_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_index_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_index_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_id_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_id_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_type_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_type_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_index_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_index_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_id_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_id_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_name_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_name_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_type_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_type_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_index_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_index_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_id_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_id_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_name_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_name_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_type_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_type_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_atoms_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_atoms_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_groups_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_groups_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_components_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_components_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_molecules_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_molecules_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_chains_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_chains_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_entities_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_entities_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_bonds_from_chain (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_bonds_from_chain as _get
    return _get(item.topology, indices=indices, check_form=False)

## entity

def get_atom_index_from_entity(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_index_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_atom_id_from_entity(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_id_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_atom_name_from_entity(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_name_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_atom_type_from_entity(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_type_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_index_from_entity(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_index_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_id_from_entity(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_id_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_name_from_entity(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_name_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_group_type_from_entity(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_group_type_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_name_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_name_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_index_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_index_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_id_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_id_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_component_type_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_component_type_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_name_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_name_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_index_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_index_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_id_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_id_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_chain_type_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_chain_type_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_index_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_index_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_id_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_id_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_name_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_name_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_molecule_type_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_molecule_type_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_index_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_index_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_id_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_id_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_name_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_name_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_entity_type_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_entity_type_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_atoms_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_atoms_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_groups_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_groups_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_components_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_components_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_molecules_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_molecules_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_chains_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_chains_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_entities_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_entities_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_bonds_from_entity (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_bonds_from_entity as _get
    return _get(item.topology, indices=indices, check_form=False)


## system

def get_bonded_atoms_from_system(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_bonded_atoms_from_system as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_atoms_from_system(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_atoms_from_system as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_groups_from_system(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_groups_from_system as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_components_from_system(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_components_from_system as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_chains_from_system(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_chains_from_system as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_molecules_from_system(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_molecules_from_system as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_entities_from_system(item, indices='all', frame_indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_entities_from_system as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_bonds_from_system(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_bonds_from_system as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_aminoacids_from_system (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_aminoacids_from_system as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_nucleotides_from_system (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_nucleotides_from_system as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_ions_from_system (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_ions_from_system as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_waters_from_system (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_waters_from_system as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_cosolutes_from_system (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_cosolutes_from_system as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_small_molecules_from_system (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_small_molecules_from_system as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_peptides_from_system (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_peptides_from_system as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_proteins_from_system (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_proteins_from_system as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_dnas_from_system (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_dnas_from_system as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_rnas_from_system (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_rnas_from_system as _get
    return _get(item.topology, indices=indices, frame_indices=frame_indices, check_form=False)

def get_n_lipids_from_system (item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_n_lipids_from_system as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_coordinates_from_system(item, indices='all', frame_indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Trajectory import get_coordinates_from_system as _get
    return _get(item.trajectory, indices=indices, frame_indices=frame_indices, check_form=False)

def get_box_from_system(item, frame_indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Trajectory import get_box_from_system as _get
    return _get(item.trajectory, frame_indices=frame_indices, check_form=False)

def get_box_shape_from_system(item, frame_indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Trajectory import get_box_shape_from_system as _get
    return _get(item.trajectory, indices=indices, frame_indices=frame_indices, check_form=False)

def get_box_lengths_from_system(item, frame_indices='all', check_form=True):

    _checking_form(item, check_form)

    tmp_box_lengths = item.trajectory.get_box_lengths()
    if tmp_box_lengths is not None and frame_indices is not 'all':
        tmp_box_lengths = tmp_box_lengths[frame_indices,:]
    return tmp_box_lengths

def get_box_angles_from_system(item, frame_indices='all', check_form=True):

    _checking_form(item, check_form)

    tmp_box_angles = item.trajectory.get_box_angles()
    if tmp_box_angles is not None and frame_indices is not 'all':
        tmp_box_angles = tmp_box_angles[frame_indices,:]
    return tmp_box_angles

def get_box_volume_from_system(item, frame_indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.pbc import box_volume_from_box_vectors
    tmp_box = get_box_from_system(item, indices=indices, frame_indices=frame_indices, check_form=False)
    return box_volume_from_box_vectors(tmp_box)

def get_time_from_system(item, indices='all', frame_indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Trajectory import get_time_from_system as _get
    return _get(item.trajectory, frame_indices=frame_indices, check_form=False)

def get_step_from_system(item, frame_indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Trajectory import get_step_from_system as _get
    return _get(item.trajectory, frame_indices=frame_indices, check_form=False)

def get_frame_from_system(item, indices='all', frame_indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Trajectory import get_frame_from_system as _get
    return _get(item.trajectory, indices=indices, frame_indices=frame_indices, check_form=False)

def get_n_frames_from_system(item, frame_indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Trajectory import get_n_frames_from_system as _get
    return _get(item.trajectory, frame_indices=frame_indices, check_form=False)

## bond

def get_bond_index_from_bond(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_bond_index_from_bond as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_bond_order_from_bond(item, indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_bond_order_from_bond as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_bond_type_from_bond(item, indices='all', frame_indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_bond_type_from_bond as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_atom_index_from_bond(item, indices='all', frame_indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.molsysmt_Topology import get_atom_index_from_bond as _get
    return _get(item.topology, indices=indices, check_form=False)

def get_n_bonds_from_bond(item, indices='all', frame_indices='all', check_form=True):

    _checking_form(item, check_form)

    from molsysmt.tools.api_molsysmt_Topology import get_n_bonds_from_bond as _get
    return _get(item.topology, indices=indices, check_form=False)

